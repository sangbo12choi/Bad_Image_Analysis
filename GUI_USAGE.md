# GUI 프로그램 사용 가이드

## 개요

`analyze_gui.py`는 tkinter를 사용한 그래픽 사용자 인터페이스(GUI) 프로그램입니다. 마우스 클릭만으로 쉽게 이미지 분석을 수행할 수 있습니다.

## 실행 방법

### 기본 실행

```bash
python analyze_gui.py
```

GUI 창이 열리면 다음과 같은 화면이 표시됩니다:

```
┌─────────────────────────────────────────────────────────────┐
│  불량별 이미지 분석 시스템                                    │
├──────────────┬──────────────────────────────────────────────┤
│ 분석 설정     │  이미지 결과 (탭)                             │
│              │  - 이미지 결과                                │
│ [이미지 선택] │  - 통계                                       │
│ [불량 유형]   │  - 리포트                                     │
│ [분석 설정]   │                                               │
│ [분석 실행]   │  [결과 이미지 표시 영역]                      │
│              │                                               │
└──────────────┴──────────────────────────────────────────────┘
```

## 주요 기능

### 1. 이미지 선택

#### 단일 이미지 분석
1. **"📁 이미지 파일 선택"** 버튼 클릭
2. 파일 브라우저에서 이미지 파일 선택
3. 선택된 파일 경로가 표시됩니다

#### 배치 처리 (폴더)
1. **"📂 폴더 선택 (배치 처리)"** 버튼 클릭
2. 폴더 선택
3. "배치 처리 모드" 체크박스가 자동으로 선택됩니다

### 2. 불량 유형 선택

분석할 불량 유형을 선택할 수 있습니다:

- ☑ **전체 불량**: 모든 유형의 불량 감지
- ☐ **Chipping**: 가장자리/모서리 결함
- ☐ **Crack**: 외곽에서 시작하는 균열
- ☐ **Scratch**: 선형 긁힘
- ☐ **Bubble**: 기포

**참고**: "전체 불량"을 선택하면 다른 옵션들이 자동으로 해제됩니다.

### 3. 분석 설정

분석 파라미터를 조정할 수 있습니다:

- **최소 결함 크기**: 감지할 최소 불량 크기 (픽셀)
  - 기본값: 10
  - 범위: 1 ~ 10,000
  
- **최대 결함 크기**: 감지할 최대 불량 크기 (픽셀)
  - 기본값: 100,000
  - 범위: 100 ~ 1,000,000

- **이진화 방법**: 이미지 이진화 알고리즘 선택
  - **adaptive**: 적응형 임계값 (기본값, 권장)
  - **otsu**: 오츠 알고리즘
  - **manual**: 수동 임계값

### 4. 분석 실행

1. 이미지 또는 폴더 선택
2. 불량 유형 선택
3. 분석 설정 조정 (선택사항)
4. **"🔍 분석 실행"** 버튼 클릭

분석이 진행되는 동안 상태바에 "분석 중..." 메시지가 표시됩니다.

### 5. 결과 확인

분석이 완료되면 세 개의 탭에서 결과를 확인할 수 있습니다:

#### 이미지 결과 탭
- 원본 이미지에 감지된 불량이 색상으로 표시됩니다
- 불량 유형별 색상:
  - Chipping: 자홍색 (magenta)
  - Crack: 청록색 (cyan)
  - Scratch: 주황색 (orange)
  - Bubble: 보라색 (purple)
- 각 불량에 바운딩 박스, 중심점, 라벨이 표시됩니다

#### 통계 탭
- 불량 유형별 분포
- 심각도별 분포
- 면적 통계 (최소/최대/평균/총 면적)
- 불량 유형별 분포 차트

#### 리포트 탭
- 상세한 텍스트 리포트
- 각 불량의 ID, 유형, 심각도, 면적, 중심점 좌표

### 6. 결과 저장

1. 분석 완료 후 **"💾 결과 저장"** 버튼 클릭
2. 저장할 폴더 선택
3. 다음 파일들이 저장됩니다:
   - `파일명_result.jpg`: 결과 이미지
   - `파일명_report.csv`: CSV 리포트

## 사용 예시

### 예시 1: 단일 이미지에서 Chipping만 분석

1. **"📁 이미지 파일 선택"** 클릭
2. `generated_images/chipping/sample_chipping.jpg` 선택
3. "Chipping (가장자리/모서리)" 체크박스 선택
4. "전체 불량" 체크박스 해제
5. **"🔍 분석 실행"** 클릭
6. 결과 확인 후 **"💾 결과 저장"** 클릭

### 예시 2: 배치 처리로 모든 불량 분석

1. **"📂 폴더 선택 (배치 처리)"** 클릭
2. `generated_images/` 폴더 선택
3. "전체 불량" 선택 (기본값)
4. **"🔍 분석 실행"** 클릭
5. 완료 메시지 확인

### 예시 3: 작은 불량 감지

1. 이미지 선택
2. 분석 설정에서 **최소 결함 크기**를 5로 변경
3. 분석 실행

## 화면 구성

### 왼쪽 패널 (분석 설정)

- **이미지 선택**: 파일 또는 폴더 선택
- **불량 유형 선택**: 분석할 불량 유형 선택
- **분석 설정**: 파라미터 조정
- **실행 버튼**: 분석 실행 및 결과 저장

### 오른쪽 패널 (결과 표시)

- **이미지 결과 탭**: 분석 결과 이미지 표시
- **통계 탭**: 통계 정보 및 차트
- **리포트 탭**: 상세 텍스트 리포트

### 하단 상태바

- 현재 상태 메시지 표시
- 분석 진행 상황 표시

## 팁

1. **이미지 크기**: 큰 이미지는 분석 시간이 오래 걸릴 수 있습니다
2. **배치 처리**: 여러 이미지를 한 번에 분석할 때는 배치 처리 모드를 사용하세요
3. **설정 조정**: 작은 불량을 감지하려면 최소 결함 크기를 낮추세요
4. **결과 저장**: 분석 후 반드시 결과를 저장하세요
5. **불량 유형**: 특정 불량만 분석하려면 해당 유형만 선택하세요

## 문제 해결

### GUI 창이 열리지 않습니다
- Python이 설치되어 있는지 확인하세요
- tkinter가 설치되어 있는지 확인하세요 (일반적으로 Python과 함께 설치됨)

### 이미지를 로드할 수 없습니다
- 파일 경로에 한글이 포함되어 있지 않은지 확인하세요
- 이미지 파일이 손상되지 않았는지 확인하세요
- 지원하는 형식인지 확인하세요 (.jpg, .jpeg, .png, .bmp, .tiff, .tif)

### 분석이 너무 느립니다
- 이미지 크기를 확인하세요 (너무 큰 이미지는 시간이 오래 걸립니다)
- 최소/최대 결함 크기 범위를 좁혀보세요
- 배치 처리 시 한 번에 너무 많은 이미지를 처리하지 마세요

### 결과가 표시되지 않습니다
- 분석이 완료될 때까지 기다리세요
- 이미지 탭을 클릭하여 결과를 확인하세요
- 통계 탭에서 통계 정보를 확인하세요

## 키보드 단축키

현재 버전에서는 키보드 단축키를 지원하지 않습니다. 향후 버전에서 추가될 예정입니다.

## 시스템 요구사항

- Python 3.7 이상
- tkinter (일반적으로 Python과 함께 설치됨)
- matplotlib
- opencv-python
- numpy
- Pillow

필요한 패키지는 `requirements.txt`에 정의되어 있습니다:

```bash
pip install -r requirements.txt
```

## 기존 프로그램과의 비교

| 기능 | 콘솔 프로그램 | GUI 프로그램 |
|------|--------------|-------------|
| 사용 편의성 | ⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| 불량 유형 선택 | ✅ | ✅ |
| 배치 처리 | ✅ | ✅ |
| 결과 시각화 | ✅ | ✅ |
| 설정 변경 | ✅ | ✅ |
| 결과 저장 | ✅ | ✅ |
| 인터랙티브 | ✅ | ✅ |

GUI 프로그램은 마우스 클릭만으로 모든 기능을 사용할 수 있어 더욱 사용하기 쉽습니다.

